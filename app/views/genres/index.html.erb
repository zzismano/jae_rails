<div class="container my-3">
  <div class="row justify-content-center">
    <div class="col-lg-8 col-md-8 col-12 bg-light rounded p-4">
      <h2 class="text-center h3 mb-3 fw-bold">Gêneros
        <%= link_to new_genre_path do %>
          <i class="fa-solid fa-circle-plus"></i>
        <% end %>
      </h2>
      <table class="table">
        <thead>
          <tr>
            <th scope="col">Nome</th>
          </tr>
        <tbody>
          <% @genres.order(:name).each do |genre|%>
            <tr>
              <td>
                <%= link_to "#{genre.name}", edit_genre_path(genre)%>
                <%= link_to genre_path(genre), data: {turbo_method: :delete, turbo_confirm: "tem certeza?"} do %>
                    <i class="fa-solid fa-delete-left"></i>
                <% end %>
              </td>
            </tr>
          <% end %>
    </div>
  </div>
</div>
